var Utils = (function () {

    const DEFAULT_OFFSET_HEIGHT = -1;

    const MIN_INTEGER_STRING = '' + Number.MIN_SAFE_INTEGER;

    return {
        DEFAULT_OFFSET_HEIGHT: DEFAULT_OFFSET_HEIGHT,
        MIN_INTEGER_STRING: MIN_INTEGER_STRING
    };

})();

export { Utils };